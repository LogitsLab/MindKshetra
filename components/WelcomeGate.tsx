"use client";

/**
 * Design v3: web has no forced onboarding gate.
 * Personalize lives under Account settings; /welcome remains an optional invite page.
 */
export default function WelcomeGate() {
  return null;
}
